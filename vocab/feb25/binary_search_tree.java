public class binary_search_tree {
  private Node root;
  
  public add(int v) {
    if (root.val == null) {
      root.val = v;
    }
    else {
      root = insert(root, v);
    }
  }

  public insert(Node n; int v) {
    n = 
  }
}
